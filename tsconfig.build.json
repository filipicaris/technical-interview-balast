{
  "extends": "./tsconfig.json",
  "exclude": ["node_modules", "test", "dist", "**/*spec.ts"],
  "include": [
    "src/**/*.ts", // Your TypeScript files
    "src/**/*.txt" // Include text files as well
  ]
}
